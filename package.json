{
    "private": true,
    "scripts": {
        "generate:sdk": "./scripts/generate-sdk.sh",
        "generate:openapi-spec": "cd packages/server && OPEN_API_GENERATION_ONLY=true yarn start"
    },
    "workspaces": [
        "packages/*"
    ]
}